type: 'application'
language: 'typescript'

project:
  name: 'eva-uni'
  description: 'eva-uni'
  owner: 'freeaoi'
  channel: '#evauni'
  maintainers: []

tasks:
  build:
    command: 'swc src -d build'
    inputs:
      - 'src/**/*'
    outputs:
      - 'build'

  run:
    command: 'node build/index.js'
